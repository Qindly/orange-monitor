# When running server on host and PostgreSQL in docker-compose:
DATABASE_URL=postgresql://orange_monitor:orange_monitor@localhost:5432/orange_monitor?schema=public

# If you run server inside Docker network, switch host to `postgres`:
# DATABASE_URL=postgresql://orange_monitor:orange_monitor@postgres:5432/orange_monitor?schema=public
